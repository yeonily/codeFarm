<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">

<select id = "checkId" parameterType="string" resultType="_int">
 select count(member_id) from tbl_member where member_id = #{memberId}
</select>

<select id="checkEmail" parameterType="string" resultType="_int">
	select count(member_email) from tbl_member where member_email = #{memberEmail}
</select>

<select id="checkPhoneNumber" parameterType="string" resultType="_int">
	select count(member_phone_number) from tbl_member where member_phone_number = #{memberPhoneNumber}
</select>

</mapper>





















